def slice(s: str, start, end: int) -> str:
    x = 4  # type: int
    y = [3, 3]
    if start < 0:
        help: int = 0
        sos = False  # type: bool
    return "a"


def test(x):  # type: int -> str
    return "b"
