def slice(s: str, start, end: int) -> str:
    x = 4  # type: int
    y = [3, 3]
    if start < 0:
        help: int = 0
        sos = False  # type: bool
    return s[start:end]


def test(x: 'int'):
    y: 'int'
    return len(x)


def hints(y):  # type: int -> str
    z = 0  # type: int
    s, t = "a", "b"
    return ""


class Color(NamedTuple):
    color: str
